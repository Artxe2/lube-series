{
	"author": "artxe2",
	"bugs": {
		"url": "https://github.com/Artxe2/lube-series/issues"
	},
	"exports": {
		".": {
			"import": "./src/index.js",
			"require": "./async-lube.cjs",
			"types": "./types/index.d.ts"
		}
	},
	"description": "Simplify Asynchronous Operations in JavaScript",
	"keywords": [
		"async",
		"lube",
		"async-lube",
		"dag",
		"decorator",
		"parallel",
		"promise",
		"syntax sugar"
	],
	"license": "MIT",
	"main": "src/index.js",
	"name": "async-lube",
	"repository": {
		"type": "git",
		"url": "https://github.com/Artxe2/lube-series",
		"directory": "packages/async-lube"
	},
	"scripts": {
		"clean:win": "node -e \"if (process.platform === 'win32') process.exit(1)\" || , if exist types rmdir /Q /S types",
		"clean:lin": "node -e \"if (process.platform !== 'win32') process.exit(1)\" || rm -rf types",
		"cjs": "rollup src/index.js --file async-lube.cjs --format cjs",
		"dts": "npm run clean:win && npm run clean:lin && tsc && cd ../.. && node scripts/async-lube_fix_types.js",
		"test": "cd ../../test/async-lube/ && vitest run"
	},
	"type": "module",
	"types": "types/index.d.ts",
	"version": "1.5.2",
	"devDependencies": {
		"rollup": "^4.1.4"
	}
}